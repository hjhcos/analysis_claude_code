# MCP智能搜索AI规则
# 当项目内容不足时自动触发网络搜索

## 核心规则：智能上下文搜索策略

当用户提出技术问题时，请按以下优先级顺序执行：

### 1. 项目内容优先搜索
首先在当前项目中搜索相关内容：
- 使用 `grep` 工具搜索关键词
- 检查文档、代码文件、配置文件
- 查找相关的实现、示例或文档

### 2. 触发网络搜索的条件
当满足以下任一条件时，自动使用 `web_search` 工具进行网络搜索：

**条件A - 项目内容不足：**
- 项目搜索结果少于3个相关匹配
- 搜索结果与用户问题相关度低
- 项目中缺乏相关技术栈的实现

**条件B - 特定技术问题：**
- 用户询问最新技术趋势或版本信息
- 询问最佳实践、性能优化方案
- 涉及第三方服务集成或配置
- 数据库相关操作（如：数据库热部署、迁移、性能调优）
- DevOps和部署相关问题

**条件C - 实时信息需求：**
- 需要最新的API文档
- 框架版本兼容性问题
- 安全漏洞或更新信息

### 3. 搜索策略执行流程

```
用户问题 → 项目内搜索 → 评估结果充分性 → 
如果不足 → 网络搜索 → 综合项目和网络信息 → 提供完整答案
```

### 4. 网络搜索关键词优化
使用 `web_search` 时，优化搜索词：
- 包含具体技术栈名称和版本
- 添加 "best practices"、"tutorial"、"how to" 等修饰词
- 结合项目上下文，如编程语言、框架等

### 5. 响应格式要求
当使用网络搜索补充信息时：
1. 先说明在项目中找到的相关内容（如果有）
2. 标明使用网络搜索获取补充信息
3. 提供综合的、完整的解决方案
4. 如果可能，建议如何在当前项目中应用

### 6. 示例触发场景
- "如何实现数据库热部署" → 项目搜索database/deploy → 结果不足 → 网络搜索"database hot deployment best practices"
- "Redis集群配置优化" → 项目搜索redis/cluster → 无相关内容 → 网络搜索"Redis cluster optimization configuration"
- "Docker容器性能监控" → 项目搜索docker/monitor → 内容有限 → 网络搜索"Docker container performance monitoring tools"

### 7. MCP工具使用优先级
1. 优先使用 `grep` 进行项目内搜索
2. 使用 `read_file` 查看相关文件
3. 条件满足时使用 `web_search` 进行网络搜索
4. 必要时使用其他MCP工具（如playwright相关工具）

### 8. 避免过度搜索
- 对于简单的代码语法问题，优先使用已知知识
- 对于项目特定的配置问题，重点搜索项目内容
- 只在确实需要外部信息时才进行网络搜索

这个规则确保AI助手能够智能地判断何时需要补充外部信息，提供更全面、准确的技术支持。