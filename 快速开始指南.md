# MCP智能搜索规则 - 快速开始指南

## 概述

这是一个MCP（Model Context Protocol）智能搜索规则系统，能够：

1. **智能判断**：检测用户查询在项目中是否有相关内容
2. **上下文感知**：分析技术领域和关键词
3. **自动降级**：项目内无相关内容时自动启用网络搜索
4. **结果融合**：整合项目内容和网络搜索结果

## 快速安装

### 步骤1：安装依赖

```bash
# 安装Node.js依赖
npm install

# 安装Playwright浏览器
npm run install-browsers
```

### 步骤2：配置MCP服务器

项目已包含 `.mcp.json` 配置文件：

```json
{
  "mcpServers": {
    "smart_search": {
      "type": "stdio",
      "command": "node",
      "args": ["./smart-search-mcp-server.js"],
      "env": {
        "NODE_ENV": "production",
        "PLAYWRIGHT_BROWSERS_PATH": "./browsers",
        "CONTEXT_THRESHOLD": "0.3"
      }
    }
  }
}
```

### 步骤3：启动MCP服务器

```bash
# 生产模式
npm start

# 开发模式（包含详细日志）
npm run dev
```

## 使用示例

### 基本用法

在Claude Code中使用智能搜索工具：

```javascript
// 示例1：查询数据库热部署
const result = await smartSearch({
  query: "如何实现数据库热部署",
  domain: "database"
});

// 示例2：React组件开发
const result = await smartSearch({
  query: "React组件状态管理最佳实践",
  domain: "frontend"
});

// 示例3：强制网络搜索
const result = await smartSearch({
  query: "最新的AI技术趋势",
  forceWebSearch: true
});
```

### 响应结构

```json
{
  "success": true,
  "query": {
    "originalQuery": "如何实现数据库热部署",
    "domain": "database",
    "keywords": ["数据库", "热部署"],
    "searchTerms": ["如何实现数据库热部署", "数据库", "热部署"]
  },
  "results": {
    "hasProjectContext": false,
    "projectRelevanceScore": 0.1,
    "projectResults": [],
    "webResults": [...],
    "recommendations": [
      "在线搜索到以下相关资源:",
      "- [Google] 数据库热部署最佳实践: https://example.com/...",
      "- [StackOverflow] Database Hot Deployment: https://stackoverflow.com/..."
    ]
  },
  "strategy": "hybrid",
  "timestamp": "2024-01-15T10:30:00.000Z"
}
```

## 工作原理

### 智能决策流程

```
用户查询 → 查询分析 → 项目上下文检测 → 决策引擎 → 搜索策略
    ↓           ↓            ↓            ↓         ↓
  语义解析   关键词提取   相关性评分   阈值判断   项目内容/网络搜索
```

### 决策逻辑

1. **有项目上下文** (相关性 > 0.3)：优先展示项目内容，补充网络资源
2. **无项目上下文** (相关性 ≤ 0.3)：主要进行网络搜索
3. **强制网络搜索**：直接进行网络搜索，忽略项目上下文

### 支持的技术领域

- `database`：数据库相关技术
- `frontend`：前端开发（React、Vue、Angular等）
- `backend`：后端开发（API、微服务等）
- `devops`：DevOps和部署相关
- `ai`：人工智能和机器学习
- `general`：通用技术查询

## 自定义配置

### 调整相关性阈值

```bash
# 在.mcp.json中修改环境变量
"CONTEXT_THRESHOLD": "0.2"  # 更容易触发网络搜索
"CONTEXT_THRESHOLD": "0.5"  # 更依赖项目内容
```

### 添加自定义搜索源

编辑 `smart-search-mcp-server.js` 中的 `buildSearchUrls` 方法：

```javascript
buildSearchUrls(query, domain) {
  // 添加您喜欢的搜索引擎
  return [
    `https://www.google.com/search?q=${encodedQuery}`,
    `https://stackoverflow.com/search?q=${encodedQuery}`,
    `https://github.com/search?q=${encodedQuery}`,
    `https://dev.to/search?q=${encodedQuery}`,        // 添加Dev.to
    `https://medium.com/search?q=${encodedQuery}`     // 添加Medium
  ];
}
```

### 自定义关键词字典

在 `analyzeQuery` 方法中添加或修改技术关键词：

```javascript
const techKeywords = {
  database: ['数据库', '热部署', 'mysql', 'postgresql', 'mongodb', 'redis'],
  frontend: ['react', 'vue', 'angular', '前端', '组件'],
  // 添加您的自定义关键词
  blockchain: ['区块链', 'bitcoin', 'ethereum', 'smart contract'],
  mobile: ['android', 'ios', 'react native', 'flutter']
};
```

## 故障排除

### 常见问题

1. **浏览器启动失败**
   ```bash
   # 重新安装浏览器
   npx playwright install chromium
   ```

2. **网络搜索失败**
   - 检查网络连接
   - 确认防火墙设置
   - 验证搜索网站是否可访问

3. **项目文件搜索失败**
   - 确认文件读取权限
   - 检查项目路径是否正确

### 调试模式

启用详细日志：

```bash
NODE_ENV=development npm start
```

### 测试功能

```bash
# 运行单元测试
npm test
```

## 性能优化

### 缓存配置

系统自动缓存搜索结果1小时，减少重复请求。

### 并发控制

- 最多同时进行3个网络搜索
- 每个搜索源限制5个结果
- 搜索间隔1秒避免过快请求

### 资源管理

- 自动关闭浏览器页面
- 优雅处理进程退出
- 内存使用监控

## 扩展开发

### 添加新的搜索引擎

1. 在 `buildSearchUrls` 中添加URL
2. 在 `extractSearchResults` 中添加解析逻辑
3. 实现对应的结果提取方法

### 集成新的项目文件类型

在 `searchCodeFiles` 和 `searchDocuments` 中添加新的文件扩展名。

### 自定义相关性算法

修改 `calculateRelevanceScore` 方法实现自定义评分逻辑。

## 支持和贡献

如果您遇到问题或有改进建议，请：

1. 查看详细文档：`MCP智能搜索规则设计指南.md`
2. 检查日志输出进行调试
3. 提交Issue或Pull Request

## 许可证

MIT License - 请参阅 LICENSE 文件了解详情。